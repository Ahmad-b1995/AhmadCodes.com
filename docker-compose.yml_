services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    container_name: tokenwizards_nextjs
    ports:
      - "3001:3000"
    restart: always
    labels:
      - "service=app"
      - "stage=production"
